<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar Sesión - Rescate y Protección Animal (RPA)</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css">
    <link rel="shortcut icon" href="/miniatura.png" type="image/x-icon">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Oswald:wght@400&display=swap">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background-image: url('https://st3.depositphotos.com/1177973/14011/i/450/depositphotos_140115380-stock-photo-group-of-cute-pets.jpg');
            background-size: cover;
            background-position: center;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #333;
        }

        .container {
            background-color: rgba(255, 255, 255, 0.8);
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 2px 15px rgba(0, 0, 0, 0.2);
            text-align: center;
            width: 90%;
            max-width: 500px;
        }

        h2 {
            color: #FF5733;
            margin-bottom: 20px;
            font-family: 'Oswald', sans-serif;
            font-size: 2.8em;
        }

        input {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 1em;
        }

        button {
            padding: 10px;
            width: 100%;
            background-color: #FF5733;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: .3s;
            font-size: 1em;
            margin-top: 10px;
        }

        button:hover {
            background-color: transparent;
            color: #FF5733;
            border: 2px solid #FF5733;
        }

        a {
            color: #FF5733;
            text-decoration: none;
            font-size: 0.9em;
        }

        a:hover {
            text-decoration: underline;
        }

        .title {
            font-size: 2.8em;
        }

        .alert {
            color: red;
            font-weight: bold;
            margin-top: 10px;
        }

        .success {
            color: green;
            font-weight: bold;
            margin-top: 10px;
        }

        .admin-access {
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 1.5em;
            color: #FF5733;
            cursor: pointer;
            transition: color 0.3s;
        }

        .admin-access:hover {
            color: #333;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Rescate y Protección Animal (RPA)</h1>
        <form action="/login" method="POST">
            <div class="form-group">
                <label for="email">Correo Electrónico</label>
                <input type="email" id="email" name="email" required>
            </div>
            <div class="form-group">
                <label for="password">Contraseña</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit">Iniciar Sesión</button>
            <p>¿No tienes una cuenta? <a href="/register">Regístrate aquí</a></p>
        </form>

        <% if (mensaje) { %>
            <div class="<%= mensaje.type === 'success' ? 'success' : 'alert' %>">
                <%= mensaje.text %>
            </div>
        <% } %>
    </div>

    <!-- Ícono de acceso de administrador -->
    <a href="/admin-auth" class="admin-access" title="Acceso de administrador">
        <i class="fas fa-ellipsis-h"></i>
    </a>
</body>
</html>
